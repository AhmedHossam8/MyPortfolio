# Personal Portfolio

Ahmed Emara — Junior Software Engineer portfolio built with React + Vite.

## Development

```bash
npm run dev
```

Opens at `http://localhost:5173/`.

## Build

```bash
npm run build
```

Output in `dist/`.

## Deploy

Auto-deploys via Netlify. Any push to `main` triggers a new build.
